# Couche de mock MSW. Mettre `disabled` une fois le backend de Thư disponible.
NEXT_PUBLIC_API_MOCKING=enabled

# URL du backend réel. Vide = requêtes relatives interceptées par MSW.
NEXT_PUBLIC_API_BASE_URL=
